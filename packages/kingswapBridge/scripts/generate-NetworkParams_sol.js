#!/usr/bin/env node
const { writeContent } = require('../../../scripts/shared/generateSol.lib');

writeContent `// Auto-generated by \`../scripts/generate-NetworkParams_sol.js\` (updates in this file will be lost).
// SPDX-License-Identifier: MIT
pragma solidity >=0.6.0 <0.8.0;

/// @dev Network-dependant params (i.e. addresses, block numbers, etc..)
contract NetworkParams {
    // Layer1 chain: "${'layer1 chain'}", side-chain: "${'side-chain'}" 

    // Arbitrary Message Bridge (AMB) contract on the layer1 chain
    address internal constant ambAddress = ${'ambAddress'};
    // Arbitrary Message Bridge (AMB) contract on the side-chain
    address internal constant xAmbAddress = ${'xAmbAddress'};

    // $KING token contract address on the layer1 chain
    address internal constant kingTokenAddress = ${'kingTokenAddress'};
    // x$KING token (proxy) contract on the side-chain (replica of the $KING)
    address internal constant xKingTokenAddress = ${'xKingTokenAddress'};
}
`([__dirname, '../contracts/NetworkParams.sol'])
